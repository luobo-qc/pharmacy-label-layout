# 药房药品标签排版

一个前端网页工具，用于手动录入患者用药信息，实时预览 `60mm x 40mm` 药品标签，并通过浏览器打印到热敏或标签打印机。

## 功能

- 患者用药标签手动录入
- `60mm x 40mm` 标签实时预览
- 浏览器打印样式
- 模板设置：字号、药品名称加粗、注意事项显示、标签内边距
- 浏览器本地保存常用项和最近打印记录
- 长文本打印前校验

## 开发

```bash
npm install
npm run dev
```

## 验证

```bash
npm test
npm run build
```
